---
record_id: 260718-gaps-5-7
session: Claude(Fable 5)
date: 2026-07-18
repos: [flightwake]
tests: smoke 全過(15 項,含本次新增 uninstall×2、--ci、monorepo case)
prod_changes: none
---

# 開源前缺口 5–7:uninstall、CI 端 STATE 檢查、monorepo 政策——缺口全清

**TL;DR**:接續同日缺口 1–4,把最後三項落地:uninstall 反向清除固定寫入範圍(使用者資料預設保留、--purge 才刪)、state-check.mjs 加 --ci 雙模式把收尾紀律帶進 CI(本 repo ci.yml 已 dogfood)、monorepo 政策定為單 repo 一份裝 git root(子目錄擋下指路)。版本 0.5.0 → 0.7.0,**開源前缺口 1–7 全部完成**,剩開源 checklist 收殘(見 STATE)。

## 關鍵發現(重要性排序)

1. **可逆性是在 init 時就買好的**:uninstall 幾乎零設計成本,因為 --private 時每一筆寫入都已用標記區塊包裹、寫入範圍從一開始就固定——「清空才刪檔」一條規則就能區分 flightwake 建的檔和使用者的檔。
2. **雙模式比雙份邏輯好**:CI 檢查與 Stop hook 是同一個 rev-list 判斷,--ci flag 只切換輸出格式與 exit code(hook 永遠 exit 0 不擋 session;CI 落後 exit 1 擋 merge)。CI 端要 fetch-depth: 0,淺 clone 數不了落後量。
3. **monorepo 政策的軸心是「工作是 session 形狀的」**:session 常跨 package,子目錄各裝會切碎 record、並讓「讀哪份 STATE」成為新的冷啟動歧義;政策與 why 見 DECISIONS 2026-07-18(三條:uninstall、CI、monorepo)。

## 交付 / Commits

`9ce619a..74d6ef9`(3 commits):uninstall 子指令 + smoke×2、--ci 雙模式 + ci.yml state-fresh job + README 範例、monorepo 擋下指路 + README 政策段。

## 驗證證據

- `bash test/smoke.sh` → `✅ smoke 全過`,15 項;最嚴斷言:--private 裝完 → uninstall --purge → `git status --porcelain` 零痕跡;使用者自己的 Stop hook 在 uninstall 後原樣保留
- `node .flightwake/hooks/state-check.mjs --ci` 本機實跑:乾淨時 `✅ 落後 0 < 門檻 3` exit 0;smoke 斷言落後 3 時 exit 1
- ci.yml 的 state-fresh job 尚未在 GitHub Actions 實跑(下次 push 遠端時看首次結果)

## 未完 / 交接

- 開源 checklist 收殘與內部試跑事項:入口見 STATE(唯一來源,此處不重抄)
- state-fresh job 首次遠端執行結果待確認(fetch-depth: 0 在 Actions 上的實際行為)
