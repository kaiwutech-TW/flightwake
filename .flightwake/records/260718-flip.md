---
record_id: 260718-flip
session: Claude(Fable 5)
date: 2026-07-18
repos: [flightwake]
tests: smoke 全過(16 項);公開 repo 首日 CI 三 run 全綠(ci + scorecard×2)
prod_changes: "開源上線:公開 repo + npm 發布 + trusted publisher(對外不可逆等級的變更,證據見下)"
---

# 開源上線:npm 佔位 → 四語 README → flip runbook 執行完畢

**TL;DR**:依「兩 gate 關掉即上」的提前決策(DECISIONS 2026-07-18),當日完成:npm `flightwake@0.7.1` 手動發布佔位(2FA OTP)→ README 重構為英文主版 + zh-TW/zh-CN/ja 四語互鏈、使用者優先排序(安裝/教學置頂)、補「為什麼存在」論述 → flip runbook 七步走完(舊 repo 封存 `flightwake-archive`、新公開 repo 佔回原名甩掉伺服器端舊 objects、PVR、branch protection、trusted publisher 登記、Scorecard badge)。**專案已公開**。宣傳前只剩 gate 2(第二 repo 冷啟動實測)。

## 關鍵發現(重要性排序)

1. **新 repo 佔用舊名會切斷 GitHub rename redirect**:舊 repo 改名後 redirect 存在,但用原名建新 repo 即奪回名字——local remote URL 完全不用改,直接 push 就到新 repo。這是「甩伺服器端舊 objects」的最低摩擦路徑。
2. **npm 已強制 publish 時 2FA OTP**(登入狀態也一樣),且 2FA-bypass token 分階段退場(2026-08 敏感操作、2027-01 直接發布)——trusted publishing(OIDC)是唯一免 OTP 的自動化路徑,release.yml 方向正確。npm 網頁與 CLI 的 session 分開,網頁操作需另行登入。
3. **半套 i18n 比不翻更糟**:只翻程式註解但 CLI 輸出/skill/模板/活範例全是中文會變成混合語言;決策是註解不動,完整 i18n(CLI 輸出 + --lang)列 v0.8。英文 README 有誠實註記。
4. **Claude 署名維持**:對這個專案,git history 的 co-author 與 records 的 `session: Claude(Fable 5)` 就是產品證言,藏了反而不一致。
5. trusted publisher 表單的「Allowed actions」必勾 `Allow npm publish`(stage publish 是另一種人工核准流程)。

## 交付 / Commits

`354a995..6404724` + flip 操作(不在 git 內):npm 發布、README 四語、repo 公開化、GitHub/npm 設定。

## 驗證證據(prod 級變更)

- npm:`npm view flightwake` → 0.7.1 / MIT / deps: none,shasum 與本地 dry-run 一致;乾淨 repo `npx flightwake@0.7.1 init` 實裝全對
- GitHub:`gh repo view` → PUBLIC / default main;archive → PRIVATE;PVR enabled=true;branch protection allow_force_pushes=false
- CI:公開後三 run 全綠(run 29645123206 等),Scorecard 因轉公開開始執行
- trusted publisher:npmjs.com 表單完成(kaiwutech-TW/flightwake + release.yml + Allow npm publish)——全鏈路待首個 Release 實證

## 未完 / 交接

- gate 2 與 Release/宣傳順序:見 STATE 下一步入口
- npm 網頁端 trusted publishing 尚未實跑過一次 Release,首發時注意 workflow 是否被 npm 端正確識別
- 四語 README 同步義務:改任一版必同步其他三版(各版檔頭有註記)
